name: Build TafsirMu Assets

on:
  push:
    paths:
      - 'data/tafsir-40-targets.json'
      - 'scripts/build_tafsir_catalog_v2.py'
      - '.github/workflows/build-tafsir-assets.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install document tools
        run: |
          sudo apt-get update
          sudo apt-get install -y libreoffice poppler-utils
          python -m pip install --upgrade pip
          python -m pip install requests pypdf pymupdf python-docx

      - name: Build 40 verified TafsirMu books
        run: python scripts/build_tafsir_catalog_v2.py

      - name: Publish TafsirMu assets
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/tafsir-assets.json media/tafsir
          git diff --cached --quiet && exit 0
          git commit -m 'Build 40 complete TafsirMu books'
          git pull --rebase origin main
          git push origin HEAD:main
