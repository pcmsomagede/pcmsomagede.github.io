const CACHE='pcm-somagede-v4';
const ASSETS=['/','/index.html','/style.css','/script.js','/hero.jpg','/pimpinan-1.png','/pimpinan-2.png','/pimpinan-3.png','/pimpinan-4.png','/pimpinan-5.png','/pimpinan-6.jpeg','/pimpinan-7.jpeg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS).catch(()=>{})).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin===location.origin){event.respondWith(caches.match(event.request).then(hit=>hit||fetch(event.request).then(res=>{if(res.ok)caches.open(CACHE).then(c=>c.put(event.request,res.clone()));return res}).catch(()=>caches.match('/'))));return;}if(url.origin==='https://api.aladhan.com'){event.respondWith(fetch(event.request,{cache:'no-store'}).then(res=>{caches.open(CACHE).then(c=>c.put(event.request,res.clone()));return res}).catch(()=>caches.match(event.request)||new Response('{}',{headers:{'Content-Type':'application/json'}})));}});